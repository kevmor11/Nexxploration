<!DOCTYPE html>
<html>
  <head>
    <% include ../partials/_head %>
  </head>
  <body>
    <section class="section intro">
      <div class="container">
        <h1>NEXXPLORATION</h1>
        <p class="desc">Upcoming Space Launch Missions</p>
      </div>
    </section>

    <section class="timeline">
      <ol>
        <% launches.forEach((launch) => { %>
          <li>
            <div>
              <time>
                <img src="https://d30y9cdsu7xlg0.cloudfront.net/png/783442-200.png" class='ship-icon'>
                <a href="<%= launch.rocket.wikiURL %>"><%= launch.rocket.name %></a>
              </time>
              <i class="fa fa-thumb-tack tack" aria-hidden="true"></i><a href="<%= launch.rocket.agencies[0].wikiURL %>" class="url"> <%= launch.rocket.agencies[0].name %></a>
              <p class="location"><i class="fa fa-location-arrow" aria-hidden="true"></i> <%= launch.location.name %></p>
              <p class="location"><i class="fa fa-clock-o" aria-hidden="true"></i> <%= launch.net.substring(0, launch.net.indexOf(':') + 3) %></p>
            </div>
          </li>
        <% }); %>
      </ol>

      <div class="arrows">
        <button class="arrow arrow__prev disabled" disabled>
          <img src="images/arrow_prev.svg" alt="prev timeline arrow">
        </button>
        <button class="arrow arrow__next">
          <img src="images/arrow_next.svg" alt="next timeline arrow">
        </button>
      </div>
    </section>
    <script type="text/javascript" src="/javascripts/script.js"></script>
  </body>
</html>
