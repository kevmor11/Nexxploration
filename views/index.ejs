<!DOCTYPE html>
<html>
  <head>
    <% include ../partials/_head %>
  </head>
  <body>
    <% include ../partials/_header %>
    <section class="section intro">
      <div class="container">
        <h1>NEXXPLORATION</h1>
        <p style="margin-left: -4px;">Upcoming Space Launch Missions</p>
      </div>
    </section>

    <section class="timeline">
      <ol>
        <% launches.forEach((launch, i) => { %>
          <li>
            <div>
              <time><a href="<%= launch.rocket.wikiURL %>"><%= launch.rocket.name %></a></time>
              <i class="fa fa-thumb-tack" style="margin-left: 2px" aria-hidden="true"></i><a href="<%= launch.rocket.agencies[0].wikiURL %>" style="margin-left: 2px"> <%= launch.rocket.agencies[0].name %></a>
              <p><i class="fa fa-location-arrow" aria-hidden="true"></i> <%= launch.location.name %></p>
              <p><i class="fa fa-clock-o" aria-hidden="true"></i> <%= launch.net.substring(0, 22) %></p>
            </div>
          </li>
        <% }); %>
        <li></li>
      </ol>

      <div class="arrows">
        <button class="arrow arrow__prev disabled" disabled>
          <img src="images/arrow_prev.svg" alt="prev timeline arrow">
        </button>
        <button class="arrow arrow__next">
          <img src="images/arrow_next.svg" alt="next timeline arrow">
        </button>
      </div>
    </section>
    <script type="text/javascript" src="/javascripts/script.js"></script>
  </body>
</html>
